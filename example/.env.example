# LLM provider API key (Anthropic, Gemini, Groq, OpenRouter, OpenAI, …)
LLM_PROVIDER_API_KEY=

# Sepolia deployment (only needed for `hardhat ignition deploy --network sepolia`)
SEPOLIA_RPC_URL=
PRIVATE_KEY=
